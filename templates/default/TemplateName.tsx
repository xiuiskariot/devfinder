import styles from '../default/TempateName.module.scss';

interface TemplateNameProps { }

export const TemplateName = ({ }: TemplateNameProps) => (
  <div className={styles.templateName}>
    TemplateName Component
  </div>
);
